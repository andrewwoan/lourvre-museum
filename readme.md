# Search package.json for dependencies and installs them, a node_modules folder will show up (only do this once on download)

npm install

# Run Local Dev Server

npm run dev

# Build for production, output will be shown in dist folder

npm run build
